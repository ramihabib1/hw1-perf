# personal/local Claude Code settings (not committed)
.claude/settings.local.json

# sysbench working sets (large, regenerable)
v1/
v2/
test_file.*

# editor/OS noise
*.swp
.DS_Store

# SECRETS — never sync VM credentials to any remote
*.returned.md

# lecture PDFs: local-only, VM doesn't need them, keeps the sync repo light
*.pdf
writeup/SUBMISSION.html

# but DO track the final submission PDF (deliverable)
!writeup/SUBMISSION.pdf
